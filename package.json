{
  "name": "solid-signal-test",
  "version": "0.0.0",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf --glob '{.,examples/**,test/**}/*.d.{ts,ts.map}'",
    "test": "web-test-runner"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@web/dev-server-import-maps": "^0.2.1",
    "@web/test-runner": "^0.20.2",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "solid-js": "^1.9.9",
    "typescript": "^5.9.2"
  }
}
